package com.jacobfromnm.click;

// Minecraft imports...
import net.minecraftforge.api.distmarker.Dist;
import net.minecraftforge.fml.common.Mod;

/**
 * Handles client tick events to determine when to play the click sound.
 */
@Mod.EventBusSubscriber(modid = "click", value = Dist.CLIENT)
public class ClientTickHandler {
    // placeholder for now...
}
